1. AWK script to display the word frequency of a text
//////////////////////////////////////////////////////////////////////////////////
awk '
{
  for (i = 1; i <= NF; i++) {
    word = $i
    gsub(/[^a-zA-Z0-9]/, "", word)
    freq[word]++
  }
}
END {
  for (w in freq) {
    print w, freq[w]
  }
}' yourfile.txt
//////////////////////////////////////////////////////////////////////////////////


2. AWK script to count repetitions of a specific string and print them in reverse
//////////////////////////////////////////////////////////////////////////////////
awk -v str="yourstring" '
{
  for (i = NF; i >= 1; i--) {
    if ($i == str) {
      count++
      rev = $i " " rev
    }
  }
}
END {
  print "Count:", count
  print "Reverse:", rev
}' yourfile.txt
//////////////////////////////////////////////////////////////////////////////////


3.Using sed to rewrite phone numbers (e.g., 123456789 → (123)456-789)
//////////////////////////////////////////////////////////////////////////////////
echo "123456789" | sed -E 's/([0-9]{3})([0-9]{3})([0-9]{3})/(\1)\2-\3/'
//////////////////////////////////////////////////////////////////////////////////


4. Using sed to convert .html filenames into uppercase with .HTM suffix
//////////////////////////////////////////////////////////////////////////////////
ls *.html | sed -e 's/.html$//' | sed -e 'y/abcdefghijklmnopqrstuvwxyz/ABCDEFGHIJKLMNOPQRSTUVWXYZ/' -e 's/$/.HTM/'
//////////////////////////////////////////////////////////////////////////////////


5. Using sed to extract full user names from /etc/passwd
//////////////////////////////////////////////////////////////////////////////////
sed -n 's/^[^:]*:[^:]*:[^:]*:[^:]*:[^:]*\([^:]*\):.*$/\1/p' /etc/passwd
//////////////////////////////////////////////////////////////////////////////////


